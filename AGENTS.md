# Repository Workflow

- Implement every goal in a new Git worktree; do not make goal changes in the existing checkout.
- After completing a goal, commit the changes, push the worktree branch, and open a pull request.
- Do not merge pull requests unless the user explicitly asks for the merge.
- Keep the worktree and pull request available for review unless the user requests cleanup.
